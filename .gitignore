# macOS junk
.DS_Store

# Connect IQ build cache / generated metadata (regenerated by monkeyc on every build)
bin/gen/resources/cache/
bin/fr955-settings.json

# Font source files — downloaded from Google Fonts / Material Symbols.
# See tools/gen_bmfont.py for the exact files needed and their source URLs.
design/fonts-src/
