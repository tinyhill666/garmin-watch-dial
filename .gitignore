# macOS junk
.DS_Store

# Connect IQ build outputs / generated metadata (regenerated by monkeyc)
**/bin/
**/gen/resources/cache/
**/*-settings.json

# Font source files — downloaded from Google Fonts / Material Symbols.
# See CLAUDE.md for the exact files needed and their source URLs.
design/fonts-src/
